---
- name: Install Python versions via uv
  ansible.builtin.command:
    cmd: "{{ uv_bin }} python install {{ item }}"
  loop: "{{ python_versions }}"
  register: uv_python_install
  changed_when: "'Installed' in uv_python_install.stdout"
  tags:
    - python
